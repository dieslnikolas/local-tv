# ═══════════════════════════════════════════════
#  Environment config – copy to .env and edit!
#  cp .env.example .env
# ═══════════════════════════════════════════════

# Timezone
TZ=Europe/Prague

# Local IP address of this PC
# Find it via:  ipconfig  (look for "IPv4 Address" under WiFi or Ethernet)
HOST_IP=192.168.xxx.yyy

# ───────────────────────────────────────────────
#  Media directory
#  Use absolute path with forward slashes
#  Examples:
#    D:/Media
#    C:/Users/name/Videos
# ───────────────────────────────────────────────

# Root folder for movies, shows and downloads
# Structure: MEDIA_DIR/movies, /shows, /downloads
MEDIA_DIR=C:/Media

# ───────────────────────────────────────────────
#  API keys for Homepage dashboard widgets
#  See README for where to find each key
# ───────────────────────────────────────────────

# Radarr → Settings → General → Security → API Key
RADARR_API_KEY=

# Sonarr → Settings → General → Security → API Key
SONARR_API_KEY=

# Prowlarr → Settings → General → Security → API Key
PROWLARR_API_KEY=

# Jellyfin → Dashboard → API Keys → add new
JELLYFIN_API_KEY=

# qBittorrent → Settings → Web UI → Authentication → password
QBIT_PASSWORD=

# Bazarr → Settings → General → Security → API Key
BAZARR_API_KEY=

# ───────────────────────────────────────────────
#  System user IDs (leave as 1000 on Windows)
# ───────────────────────────────────────────────
PUID=1000
PGID=1000
